<%- include('header'); -%>
  <h1>Liste:</h1>
  <% if (items.length) { %>
    <% items.forEach(function(item) { %>
      <div>
        <%= item.category %>: <%= item.name %>
        <% if (item.protein == "oui") { %>
           (P)
        <% } %> 
        / <%= item.color %>
      </div>
    <% }) %>
  <% } else { %>
  Rien !
  <% } %>
<%- include('footer'); -%>
